meta {
  name: create user
  type: http
  seq: 1
}

post {
  url: {{host}}/users
  body: json
  auth: inherit
}

body:json {
  {
    "username": "ghostbuster",
    "email": "email@mail.com",
    "password": "Contrase;a"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
